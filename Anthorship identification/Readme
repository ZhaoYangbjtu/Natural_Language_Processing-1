Poetry Classifier: The aim of this project is to use Naive Bayes Classifier to classify some poetries between Emily Bronte and William Shakespeare. Feature set including the length of each line of poetry, average word length, and n-gram of the poetry(n:1-5). And I also including some pos-tagging(using NLTK package) codes to form the feature set.
